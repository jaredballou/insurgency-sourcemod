#include "templates/header.tmpl"

#for $name in $sortedKeys
 * <a href='#$name'>${plugins[$name].myinfo["name"]} ${plugins[$name].myinfo["version"]}</a>
#end for

#for $name in $sortedKeys
#include raw "../doc/"+str($name)+".md"
#end for

#include "templates/footer.tmpl"
